# ─── Build / deps / env ───────────────────────────────────────────────
node_modules/
.next/
out/
build/
tsconfig.tsbuildinfo
.env
.env.local
*.log
.DS_Store
.idea/

# ─── Secrets & demo credentials (NEVER commit) ────────────────────────
scripts/rls-smoke.accounts.json
scripts/bulk-onboard.mjs
supabase/seed.ts
supabase/reset.sql
onboard-credentials.csv
DEMO_ACCOUNT*.txt
# Real production credentials (NOT demo values) — move into a password manager
# and delete the file. Never committed, never pasted into a chat transcript.
PROD_CREDENTIALS.txt

# ─── Internal documentation (PRIVATE — public repo) ───────────────────
# One rule covers ALL internal docs, notes and the archive.
# Map + reading order: docs/internal/00_INDEX.md
docs/internal/
#   Defense-in-depth: if any of these are ever recreated at the repo root
#   (wrong place), keep them out of the public repo anyway.
PROJECT.md
ROADMAP.md
SECURITY_AND_INFRA.md
SALES_READINESS.md
FEATURE_REVIEW.md
GDPR_NOTES.txt
INTERVIEW_*.md
CODE_SCREEN_*.md

# ─── Real client data (PRIVATE — GDPR) ────────────────────────────────
# Per-tenant working files: class lists, contact details, rapport templates.
# This holds REAL personal data of minors and their parents. It was sitting
# untracked-but-not-ignored on 2026-08-10, i.e. one `git add -A` away from a
# public repo. Root-anchored so it cannot swallow an app route named Tenants/.
/Tenants/

# ─── Legal contract drafts (PRIVATE) ──────────────────────────────────
# Leading slash matters: unanchored `legal/` also swallowed app/legal/, the
# PUBLIC route that renders the privacy statement for visitors without an account.
/legal/

# ─── Local-only dev tooling (run-app walkthrough skill + Playwright) ──
.claude/skills/run-app/

# ─── Local references, test data, misc artifacts ──────────────────────
-aankodigingen per klas (filter).txt
csv_test.txt
test_import.csv
test.ts
Smartschool-Privacyverklaring.pdf
Rapport_template.pdf
Schermafbeelding*.png
healthy_unhealthy.png
SECURITY
